program Truth_machine;
var truth: integer;
begin
    readLn(input, truth);
    while true do
    begin
        writeLn (output, truth);
        if truth = 0 then
        begin
            break;
        end;
    end;
end.
